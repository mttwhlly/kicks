import type { RouteObject } from 'react-router';

// This file supplements the file-based routing system
// Most routes should be defined by files in the routes directory
// Add any additional routes or configurations here if needed
const routes: RouteObject[] = [];
export default routes;